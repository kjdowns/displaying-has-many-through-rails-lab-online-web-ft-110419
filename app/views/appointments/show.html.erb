<h1>Appointment Details</h1>
<% date_time = @appointment.appointment_datetime %>
<ul>
    <li>Scheduled for: <%= date_time.strftime("%B %d") %>, <%= date_time.strftime("%Y") %> at <%= date_time.strftime("%H:%M") %></li>
    <li>Doctor: <%= link_to @appointment.doctor.name, doctor_path(@appointment.doctor_id) %></li>
    <li>Patient: <%= link_to @appointment.patient.name, patient_path(@appointment.patient_id) %></li>
</ul>